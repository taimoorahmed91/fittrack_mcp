{
  "name": "fittrack-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal, vendor-neutral FitTrack MCP server",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "xmcp build",
    "postbuild": "node scripts/patch-xmcp-oauth-metadata.mjs",
    "dev": "xmcp dev",
    "start": "node dist/http.js",
    "test:db-read": "node --env-file=.env.local scripts/test-supabase-read.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3",
    "xmcp": "^0.6.13"
  }
}
